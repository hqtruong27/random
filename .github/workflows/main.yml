name: CI/CD Build and Deploy

on:
  push:
    branches:
      - main

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Docker login
        run: |
          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
          
      - name: Docker build and push Discord service
        run: |
          docker build -t hqtruong27/discord-service:latest -f Discord/Dockerfile .
          docker push hqtruong27/discord-service:latest

      - name: Docker build and push WebApi service
        run: |
          docker build -t hqtruong27/webapi-service:latest -f WebApi/Dockerfile .
          docker push hqtruong27/webapi-service:latest

  setup-ssh-env:
    runs-on: ubuntu-latest
    steps:
      - name: create env
        run: echo "${{ secrets.PRODUCTION_ENV }}" > .env.production
      - name: Create SSH Directory
        run: mkdir -p ~/.ssh

      - name: Copy SSH key
        run: echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa
        
  deploy:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Docker Ubuntu server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER_NAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
            docker pull hqtruong27/discord-service:latest
            docker pull hqtruong27/webapi-service:latest
            docker compose -f random/docker-compose.yml up -d
