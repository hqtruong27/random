namespace Common.Enum.Hoyoverse;

public enum GachaType
{
    Regular = 200,
    CharLimited = 301,
    Weapons = 302,
}

public enum RankType
{
    One = 1,
    Two = 2,
    Three = 3,
    Four = 4,
    Five = 5
}

public enum ItemType
{
    Character = 1,
    Weapons = 2,
}